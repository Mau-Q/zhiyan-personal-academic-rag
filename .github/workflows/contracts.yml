name: Core tests

on:
  pull_request:
    paths:
      - "contracts/**"
      - "backend/**"
      - "evaluation/**"
      - "fixtures/**"
      - "tests/**"
      - "pyproject.toml"
      - "Makefile"
      - ".github/workflows/contracts.yml"
  push:
    branches: [main]
    paths:
      - "contracts/**"
      - "backend/**"
      - "evaluation/**"
      - "fixtures/**"
      - "tests/**"
      - "pyproject.toml"
      - "Makefile"
      - ".github/workflows/contracts.yml"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install runtime and test dependencies
        run: python -m pip install '.[dev]'
      - name: Validate contracts, fixture consumer, and API
        run: make test
